{
  "private": "true",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap && lerna exec npm install",
    "clear": "rm -rf ./packages/*/dist",
    "dev": "npx rollup -w -c rollup-package.config.js --scope @prpl/$PACKAGE",
    "build:all": "npm run clear && for dir in packages/*; do package=${dir#'packages/'}; rollup -c rollup-package.config.js --scope @prpl/$package || exit 1; done && npx rollup -c rollup-client.config.js",
    "build:changed": "npm run clear && lerna changed | while read package; do npx rollup -c rollup-package.config.js --scope $package || exit 1; done && npx rollup -c rollup-client.config.js",
    "release:changed": "npm run build:changed && lerna publish",
    "release:all": "npm run build:all && lerna publish",
    "reset": "npm run clear && rm -rf ./**/.cache && rm -rf ./**/node_modules && rm -rf ./package.lock && npm run bootstrap",
    "test:open": "cypress open --project tests",
    "test": "cypress run --project tests"
  },
  "devDependencies": {
    "@lerna/filter-packages": "^4.0.0",
    "@lerna/project": "^4.0.0",
    "@rollup/plugin-commonjs": "^19.0.0",
    "@rollup/plugin-node-resolve": "^13.0.0",
    "@types/node": "^16.0.0",
    "cypress": "^8.1.0",
    "lerna": "^4.0.0",
    "minimist": "^1.2.5",
    "rollup": "^2.52.7",
    "rollup-plugin-analyzer": "^4.0.0",
    "rollup-plugin-auto-external": "^2.0.0",
    "rollup-plugin-sourcemaps": "^0.6.3",
    "rollup-plugin-typescript2": "^0.30.0",
    "typescript": "^4.3.5"
  }
}
