{
  "name": "tests",
  "version": "0.0.1",
  "description": "Tests for PRPL",
  "type": "module",
  "scripts": {
    "setup": "bash scripts/setup.sh",
    "test": "npm run test:build && npm run test:server",
    "test:build": "bash scripts/test.sh build",
    "test:server": "bash scripts/test.sh server"
  },
  "devDependencies": {
    "cssom": "^0.5.0",
    "linkedom": "^0.14.2",
    "uvu": "^0.5.3"
  }
}
