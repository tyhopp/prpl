{
  "name": "test-site-server",
  "version": "0.0.1",
  "description": "PRPL site to run tests against",
  "scripts": {
    "clear": "rimraf dist",
    "dev": "npm run clear && prpl && prpl-server"
  },
  "dependencies": {
    "@prpl/core": "file:../../../packages/core"
  },
  "devDependencies": {
    "@prpl/server": "file:../../../packages/server",
    "rimraf": "^3.0.2"
  }
}
