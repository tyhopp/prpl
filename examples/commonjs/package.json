{
  "name": "prpl-example-commonjs",
  "version": "0.0.1",
  "description": "Example site using PRPL in a CommonJS module",
  "scripts": {
    "dev": "rm -rf dist && node scripts/build.js && prpl-server",
    "build": "rm -rf dist && node scripts/build.js"
  },
  "dependencies": {
    "@prpl/core": "^0.3.3"
  },
  "devDependencies": {
    "@prpl/server": "^0.1.10"
  }
}
