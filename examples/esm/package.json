{
  "name": "prpl-example-esm",
  "private": true,
  "version": "0.0.1",
  "description": "Example site using PRPL in an ECMAScript module",
  "type": "module",
  "scripts": {
    "clear": "rimraf dist",
    "dev": "npm run clear && node scripts/build.js && prpl-server",
    "build": "npm run clear && node scripts/build.js"
  },
  "dependencies": {
    "@prpl/core": "^0.4.0"
  },
  "devDependencies": {
    "@prpl/server": "^0.2.0",
    "rimraf": "^3.0.2"
  }
}
