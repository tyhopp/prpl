{
  "name": "prpl-example-basic",
  "version": "0.0.1",
  "description": "Basic PRPL example site",
  "scripts": {
    "dev": "rm -rf dist && prpl && prpl-server",
    "build": "rm -rf dist && prpl"
  },
  "dependencies": {
    "@prpl/core": "^0.3.5"
  },
  "devDependencies": {
    "@prpl/server": "^0.1.11"
  }
}
